# Telegram Bot Configuration
# Get bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Webhook Secret (generate a random string for security)
# Use this when setting webhook: https://api.telegram.org/bot<TOKEN>/setWebhook?url=<URL>&secret_token=<SECRET>
TELEGRAM_WEBHOOK_SECRET=your-random-secret-string-here

# Firebase Admin SDK Configuration
# Download service account key from Firebase Console → Project Settings → Service Accounts
# Then convert JSON to environment variables OR use FIREBASE_SERVICE_ACCOUNT_JSON

# Option 1: Individual fields
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----\n"

# Option 2: Or use entire JSON as single variable
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# Database URL (optional, defaults to project ID)
FIREBASE_DATABASE_URL=https://your-project.firebaseio.com
